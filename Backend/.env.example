# Copy to .env on the server and fill in real values. NEVER commit the real .env.
PORT=5000

DB_HOST=localhost
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=evangadi_review

JWT_SECRET=
JWT_EXPIRES_IN=7d

NOTIFY_EMAIL=
NOTIFY_EMAIL_PASSWORD=
