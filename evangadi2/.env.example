# Build-time API base for the frontend. In Jenkins this is provided by the
# VITE_API_BASE environment variable in the Jenkinsfile (do not rely on this file in CI).
VITE_API_BASE=https://your-backend-url.example.com
